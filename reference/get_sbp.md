# Extract Sequential Binary Partition from a `complr` object.

Extract Sequential Binary Partition from a `complr` object.

## Usage

``` r
get_sbp(object)
```

## Arguments

- object:

  A `complr` object
