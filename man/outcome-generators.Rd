% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-outcome.R
\name{outcome-generators}
\alias{outcome-generators}
\alias{gen_outcome}
\alias{outcome_template}
\title{Generate Gaussian Outcomes from a Formula}
\usage{
gen_outcome(
  formula,
  coefficients = NULL,
  residual_cov = NULL,
  random_cov = NULL,
  compositional = FALSE,
  parts = NULL,
  sbp = NULL,
  total = 1,
  keep_ilr = TRUE,
  burnin = "auto",
  scale_formula = NULL,
  scale_coefficients = NULL,
  residual_cor = NULL
)

outcome_template(
  formula,
  compositional = FALSE,
  parts = NULL,
  sbp = NULL,
  total = 1,
  keep_ilr = TRUE,
  burnin = "auto",
  scale_formula = NULL,
  residual_cor = NULL
)
}
\arguments{
\item{formula}{Two-sided outcome formula. The left-hand side is a response
name or \code{cbind()}/\code{mvbind()} response vector. The right-hand side may use
ordinary model terms, random-effect terms such as \code{(1 | group_id)} or
\code{(1 + x | id | group_id)}, and simulation helpers \code{lag1(x)},
\code{within(x)}, \code{between(x)}, and \code{ar1()}.}

\item{coefficients}{Optional coefficient vector or matrix matching the
template returned by \code{outcome_template()}. If omitted, coefficients default
to zero.}

\item{residual_cov}{Residual covariance matrix for outcomes without
\code{scale_formula}.}

\item{random_cov}{Named random-effect covariance block or list of blocks
matching \code{outcome_template()}. Required when \code{formula} or \code{scale_formula}
contains random-effect terms.}

\item{compositional}{Logical; if \code{TRUE}, treat multivariate responses as ILR
coordinates and emit closed composition parts.}

\item{parts}{Character vector naming composition parts. Must have one more
entry than the number of ILR coordinates unless supplied by \code{sbp}.}

\item{sbp}{Optional sequential binary partition matrix.}

\item{total}{Positive scalar total for closed compositions.}

\item{keep_ilr}{Logical; if \code{TRUE}, emit both ILR coordinates and parts. If
\code{FALSE}, emit only parts.}

\item{burnin}{\code{"auto"} or a non-negative integer. Used to initialize
autoregressive outcome terms before the first observed row in each unit.}

\item{scale_formula}{Optional one-sided formula, or \code{sigma ~ ...}, defining
a log residual standard-deviation model.}

\item{scale_coefficients}{Optional coefficient vector or matrix for
\code{scale_formula}, matching \code{outcome_template()}.}

\item{residual_cor}{Residual correlation matrix used with \code{scale_formula}.}
}
\value{
\code{gen_outcome()} returns an \code{mlsim_generator_spec} for use in
\code{\link[=simulate_data]{simulate_data()}}. \code{outcome_template()} returns a generator specification
that emits no columns but records default coefficient, covariance, formula,
helper-column, and fitting metadata in \code{generator_metadata}.
}
\description{
Create outcome generator specifications and parameter templates for
Gaussian, multivariate Gaussian, and compositional outcomes.
}
\details{
\code{outcome_template()} is the safest way to discover the exact coefficient and
covariance names required by \code{gen_outcome()}. Outcome helper terms are
evaluated during simulation and are mapped to concrete helper columns for
fitting by \code{\link[=prepare_outcome_fit]{prepare_outcome_fit()}}.
}
\examples{
template <- simulate_data(
  n_groups = 2,
  n_per_group = 3,
  generators = list(
    x = gen_normal("x"),
    y_template = outcome_template(y ~ x + (1 | group_id))
  )
)$generator_metadata$y_template

coefficients <- template$coefficients
coefficients["(Intercept)", "y"] <- 1
coefficients["x", "y"] <- 0.5

sim <- simulate_data(
  n_groups = 2,
  n_per_group = 3,
  seed = 30,
  generators = list(
    x = gen_normal("x"),
    y = gen_outcome(
      y ~ x + (1 | group_id),
      coefficients = coefficients,
      residual_cov = template$residual_cov,
      random_cov = template$random_cov
    )
  )
)
sim$data

}
\concept{outcome generators}
