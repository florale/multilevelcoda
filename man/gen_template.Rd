% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-outcome.R
\name{gen_template}
\alias{gen_template}
\title{Create a Parameter Template for \code{gen_outcome()}}
\usage{
gen_template(
  formula,
  scale,
  burnin,
  composition = list(parts = NULL, total = 24, sbp = NULL, keep_ilr = TRUE),
  ar_stability = c("resample", "shrink", "error"),
  max_stability_attempts = 1000,
  shrink_target_radius = 0.98
)
}
\arguments{
\item{formula}{Outcome location formula. The left-hand side may be a single
outcome (\code{y}) or \code{mvbind(y1, y2, ...)}. The right-hand side may include
ordinary model terms, \code{between(x)}, \code{within(x)}, \code{ar1()}, interactions, and
one brms/lme4-style grouping term.}

\item{scale}{Required scale formula with \code{sigma} on the left-hand side, for
example \code{sigma ~ 1} or \code{sigma ~ treatment + (1 | ID)}. The scale model is
on the log conditional standard-deviation scale.}

\item{burnin}{Fixed non-negative integer burn-in length used when AR is
active. Ignored when no AR terms are present.}

\item{composition}{List controlling optional ILR back-transformation. Use
\code{parts} or \code{sbp} to request compositional output, \code{total} for the closure
total, and \code{keep_ilr} to keep ILR coordinates alongside parts.}

\item{ar_stability}{Handling for unstable AR matrices: \code{"resample"},
\code{"shrink"}, or \code{"error"}.}

\item{max_stability_attempts}{Positive integer maximum number of resampling
or shrinkage attempts.}

\item{shrink_target_radius}{Target spectral radius used by
\code{ar_stability = "shrink"}.}
}
\value{
An \code{mlsim_generator_spec} object for use in \code{\link[=simulate_data]{simulate_data()}}. It
emits no data columns and stores the parameter template in generator
metadata.
}
\description{
Build a generator that parses a \code{\link[=gen_outcome]{gen_outcome()}} specification and records a
complete parameter template without simulating outcome values. Use this
inside \code{\link[=simulate_data]{simulate_data()}} after any generators that define predictors used by
the outcome formula, especially predictors referenced by \code{between()} or
\code{within()}.
}
\details{
The generated template is stored at
\code{sim$generator_metadata[[name]]$params}, where \code{name} is the name given to
this generator in the \code{generators} list. The template must be created with
the same simulation design, previous generators, factor levels, formula,
scale formula, and composition settings that will be used for the later
\code{\link[=gen_outcome]{gen_outcome()}} call.

Template values are initialized to zero for regression, AR, and group-level
covariance parameters, and to identity for residual ILR correlations. The
object is ready to edit and pass as \code{params} to \code{\link[=gen_outcome]{gen_outcome()}}.
}
\examples{
template_sim <- simulate_data(
  n_groups = 3,
  n_per_group = 2,
  group_id = "ID",
  seed = 1,
  generators = list(
    treatment = gen_categorical(
      "treatment",
      level = "level2",
      categories = c("control", "treatment"),
      fixed_intercept = stats::qlogis(0.5)
    ),
    outcome_template = gen_template(
      mvbind(ilr1, ilr2) ~ treatment,
      scale = sigma ~ 1,
      burnin = 0,
      composition = list(parts = c("sleep", "active", "sedentary"), total = 24)
    )
  )
)

params <- template_sim$generator_metadata$outcome_template$params
params$location$beta["treatmenttreatment", "ilr1"] <- 0.2

}
\seealso{
Other predictor generators:
\code{\link{continuous-generators}},
\code{\link{count-generators}},
\code{\link[=gen_categorical]{gen_categorical()}},
\code{\link[=gen_custom]{gen_custom()}},
\code{\link[=gen_mvn]{gen_mvn()}},
\code{\link[=gen_outcome]{gen_outcome()}}
}
\concept{predictor generators}
