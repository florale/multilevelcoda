% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-generators.R
\name{gen_mvn}
\alias{gen_mvn}
\title{Generate Normal, Multivariate Normal, and Compositional Variables}
\usage{
gen_mvn(
  vars,
  level = c("single", "level2", "multilevel"),
  fixed_intercept = NULL,
  residual_cov = NULL,
  random_cov = NULL,
  ...,
  scale_fixed_intercept = NULL,
  residual_cor = NULL,
  compositional = FALSE,
  parts = NULL,
  total = 1,
  keep_ilr = TRUE,
  sbp = NULL
)
}
\arguments{
\item{vars}{Character vector naming the generated variables. For
compositional generators these are ILR coordinate names.}

\item{level}{Simulation level. \code{"single"} generates one row-level vector per
observation, \code{"level2"} generates one group-level vector and expands it to
each row in the group, and \code{"multilevel"} uses a random-intercept model.}

\item{fixed_intercept}{Identity-scale location intercept vector.}

\item{residual_cov}{Residual covariance matrix for Gaussian generators
without a row-specific scale model. For univariate generators, it may be a
scalar residual variance.}

\item{random_cov}{Group-level random-intercept covariance matrix for
multilevel generators. When \code{scale_fixed_intercept} is supplied this may be
either a location-only covariance or a joint location-scale covariance.}

\item{...}{Removed direct distribution parameters are rejected.}

\item{scale_fixed_intercept}{Optional log residual standard-deviation
intercepts.}

\item{residual_cor}{Residual correlation matrix used with
\code{scale_fixed_intercept}. Defaults to an identity correlation matrix.}

\item{compositional}{Logical; if \code{TRUE}, treat \code{vars} as ILR coordinates and
back-transform to composition parts.}

\item{parts}{Character vector naming the composition parts. Must have
\code{length(vars) + 1} entries unless supplied through \code{sbp} column names.}

\item{total}{Positive scalar total for closed compositions.}

\item{keep_ilr}{Logical; if \code{TRUE}, emit both ILR coordinates and parts. If
\code{FALSE}, emit only parts.}

\item{sbp}{Optional sequential binary partition matrix. Columns name parts;
rows define ILR balances using \code{-1}, \code{0}, and \code{1}.}
}
\value{
An \code{mlsim_generator_spec} object for use in \code{\link[=simulate_data]{simulate_data()}}.
}
\description{
Create a generator specification for univariate or multivariate Gaussian
variables, optionally transformed from ILR coordinates to compositional parts.
}
\details{
Compositional MVN generators simulate ILR coordinates first, then use the
SBP basis to transform the coordinates into positive composition parts that
sum to \code{total}.

Multilevel generators additionally emit the latent between- and
within-group components of each generated variable as visible columns.
For non-compositional variables these are \verb{<var>_between} (the group-level
mean, fixed plus random intercept) and \verb{<var>_within} (the row-level
residual), with \verb{<var> = <var>_between + <var>_within} exactly. For
compositional generators the same decomposition is emitted on the ILR
scale (\verb{<ilr>_between}, \verb{<ilr>_within}), together with the true between
composition \verb{<part>_between}, the closed back-transform of the group-level
ILR means. All generated variables are labelled with column roles so that
\code{between()} and \code{within()} terms in \code{\link[=gen_outcome]{gen_outcome()}} formulas resolve to the
latent components; roles (and hence \code{between()}/\code{within()}) apply to the
ILR coordinates of a compositional generator, not to its parts. When
\code{keep_ilr = FALSE} the observed ILR columns are dropped, but the
between/within columns and their roles are still emitted for multilevel
generators.

Multilevel generators also record their group-level random-effect draws
as per-row truth columns: \verb{.mlsim_<var>_random_intercept} for the
location intercept and, when \code{random_cov} is a joint location-scale
covariance, \verb{.mlsim_<var>_scale_random_intercept} for the scale
intercept. The other multilevel scalar generators (\code{\link[=gen_negbin]{gen_negbin()}},
\code{\link[=gen_gamma]{gen_gamma()}}, \code{\link[=gen_beta]{gen_beta()}}, \code{\link[=gen_poisson]{gen_poisson()}}, \code{\link[=gen_binomial]{gen_binomial()}}) share
this machinery and emit the same columns. See the Value section of
\code{\link[=simulate_data]{simulate_data()}} for the full naming contract.
}
\examples{
sim <- simulate_data(
  n = 4,
  seed = 2,
  generators = list(
    x = gen_mvn("x", fixed_intercept = 0, residual_cov = 1),
    z = gen_mvn(
      c("z1", "z2"),
      fixed_intercept = c(0, 0),
      residual_cov = diag(2),
      compositional = TRUE,
      parts = c("sleep", "activity", "sedentary")
    )
  )
)
sim$data
rowSums(as.matrix(sim$data[, c("sleep", "activity", "sedentary"), with = FALSE]))

}
\seealso{
Other predictor generators:
\code{\link{continuous-generators}},
\code{\link{count-generators}},
\code{\link[=gen_categorical]{gen_categorical()}},
\code{\link[=gen_custom]{gen_custom()}},
\code{\link[=gen_outcome]{gen_outcome()}},
\code{\link[=gen_template]{gen_template()}}
}
\concept{predictor generators}
