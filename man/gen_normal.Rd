% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-generators.R
\name{gen_normal}
\alias{gen_normal}
\title{Generate Normal Variables}
\usage{
gen_normal(
  vars,
  level = c("single", "level2", "multilevel"),
  mean = 0,
  sd = NULL,
  fixed_intercept = NULL,
  random_cov = NULL,
  residual_var = NULL,
  scale_fixed_intercept = NULL
)
}
\arguments{
\item{vars}{Character scalar naming the generated variable.}

\item{level}{Simulation level. \code{"single"} generates one row-level value per
observation, \code{"level2"} generates one group-level value and expands it to
each row in the group, and \code{"multilevel"} generates row-level values from a
random-intercept model.}

\item{mean, sd}{Direct mean and standard deviation for \code{"single"} and
\code{"level2"} generators. Values are recycled to the number of generated
units.}

\item{fixed_intercept}{Location intercept for multilevel generators. For
normal variables this is on the response scale.}

\item{random_cov}{Group-level random-intercept covariance. A scalar is
accepted for univariate location-only generators; a matrix is required when
location and scale random effects are modeled jointly.}

\item{residual_var}{Residual variance for multilevel normal generators when
no scale model is used.}

\item{scale_fixed_intercept}{Optional log residual standard-deviation
intercept for multilevel generators. When supplied, residual standard
deviations are \code{exp(scale_fixed_intercept + group_random_effect)}.}
}
\value{
An \code{mlsim_generator_spec} object for use in \code{\link[=simulate_data]{simulate_data()}}.
}
\description{
Create a generator specification for univariate Gaussian variables.
}
\details{
For non-multilevel normal variables, \code{mean} and \code{sd} define independent
draws directly. For multilevel variables, \code{fixed_intercept}, \code{random_cov},
and either \code{residual_var} or \code{scale_fixed_intercept} define a
random-intercept data-generating model.
}
\examples{
sim <- simulate_data(
  n_groups = 2,
  n_per_group = 3,
  seed = 1,
  generators = list(
    x = gen_normal("x", mean = 0, sd = 1),
    u = gen_normal(
      "u",
      level = "multilevel",
      fixed_intercept = 0,
      random_cov = 0.2,
      residual_var = 1
    )
  )
)
sim$data

}
\seealso{
Other predictor generators:
\code{\link{continuous-generators}},
\code{\link{count-generators}},
\code{\link[=gen_categorical]{gen_categorical()}},
\code{\link[=gen_custom]{gen_custom()}},
\code{\link[=gen_mvn]{gen_mvn()}}
}
\concept{predictor generators}
