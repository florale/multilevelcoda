% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lags.R
\name{lag_by_time}
\alias{lag_by_time}
\title{Lag Columns by Time}
\usage{
lag_by_time(
  data,
  cols,
  time,
  group = NULL,
  time_step = NULL,
  n = 1L,
  suffix = "_lag"
)
}
\arguments{
\item{data}{A data frame or \code{data.table} containing the columns to lag.}

\item{cols}{Character vector of column names to lag. Columns may be of any
atomic type.}

\item{time}{Character scalar naming the time column. Time values must be
numeric, \code{Date}, or \code{POSIXct}, contain no missing values, and be unique
within each group.}

\item{group}{Optional character scalar naming a grouping column (for
example, a participant identifier). When \code{NULL}, the default, all rows
are treated as one series.}

\item{time_step}{Optional positive scalar giving the time difference that
counts as one lag step. When \code{NULL}, the default, the step is inferred
as the smallest positive within-group difference between consecutive
time values. For \code{Date} time the unit is days; for \code{POSIXct} time the
unit is seconds. A \code{difftime} value is converted with \code{\link[=as.numeric]{as.numeric()}}.}

\item{n}{Positive whole number giving the lag order; the lag for a row at
time \code{t} is the observation at \code{t - n * time_step}. To create several
lag orders, call the function repeatedly with different \code{n} and
\code{suffix} values.}

\item{suffix}{Non-empty character scalar appended to each name in \code{cols}
to form the new lag column names. The new names must not collide with
existing columns.}
}
\value{
\code{data} with one new column per entry of \code{cols}, named
\code{paste0(cols, suffix)}, appended in the original row order. The input is
never modified in place; a data frame input returns a data frame and a
\code{data.table} input returns a new \code{data.table}. The result carries an
attribute \code{"lag_by_time"}, a list with:
\item{\code{time_step}}{The (possibly inferred) step used, as a number.}
\item{\code{time_step_inferred}}{Logical, whether the step was inferred.}
\item{\code{lag_columns}}{Named character vector mapping \code{cols} to the new
lag column names.}
\item{\code{n_gaps}}{Number of rows whose expected source time falls after
the start of the group's series but has no observation (a gap); these
rows receive \code{NA} lags.}
\item{\code{n_irregular}}{Number of rows whose time value is not (within
tolerance) a whole number of steps from the start of the group's
series; these rows receive \code{NA} lags and are never used as lag
sources.}
}
\description{
Create lagged versions of one or more columns using the actual time values
rather than row position. Each row's lag is taken from the row (within the
same group, when \code{group} is supplied) whose time value equals
\code{time - n * time_step}. When no observation exists at that time -- for
example, a skipped day in a daily diary -- the lag is \code{NA} instead of the
value from before the gap. This makes the lags robust to missing time
points, unlike positional shifts such as \code{\link[data.table:shift]{data.table::shift()}}.
}
\details{
Rows are matched on a per-group integer time grid: within each group,
time values are converted to step counts from the earliest observation,
\code{(time - min(time)) / time_step}, and a row's lag source is the row \code{n}
grid points earlier. A value is treated as on the grid when it is within
\code{1e-8 * max(1, time_step)} (on the time scale) of a whole number of
steps. Off-grid rows -- irregular spacing that is not a multiple of the
step -- silently receive \code{NA} lags and are counted in \code{n_irregular}; if
this affects many rows, supply the intended \code{time_step} explicitly.

Gaps are silent by design: no message or warning is emitted when a lag is
\code{NA} because the previous time point is missing. Inspect the
\code{"lag_by_time"} attribute to see how many rows were affected.

When \code{time_step} is inferred, at least one group must contain two or more
time points; otherwise an error asks for an explicit \code{time_step}.
Duplicate time values within a group are an error.
}
\examples{
# day 3 is missing, so the lag on day 4 is NA rather than the day-2 value
d <- data.frame(
  id = c(1, 1, 1, 2, 2, 2),
  day = c(1, 2, 4, 1, 2, 3),
  y = c(10, 11, 12, 20, 21, 22)
)
lag_by_time(d, cols = "y", time = "day", group = "id")

# Date time with an explicit one-day step
d2 <- data.frame(date = as.Date("2026-01-01") + c(0, 1, 3), y = 1:3)
lag_by_time(d2, cols = "y", time = "date", time_step = 1)

}
\seealso{
\code{\link[=prep_sim_analysis]{prep_sim_analysis()}}, which uses \code{lag_by_time()} to build its
\code{ar1()} lag columns.
}
