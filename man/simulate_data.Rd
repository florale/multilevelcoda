% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-data.R
\name{simulate_data}
\alias{simulate_data}
\title{Simulate Data from Generator Specifications}
\usage{
simulate_data(
  generators,
  n = NULL,
  n_groups = NULL,
  n_per_group = NULL,
  group_id = "group_id",
  obs_id = "obs_id",
  time_id = NULL,
  time_values = NULL,
  time_truncate = TRUE,
  seed = NULL
)
}
\arguments{
\item{generators}{Non-empty named list of generator specifications created
by \verb{gen_*()} functions.}

\item{n}{Total number of observations for a single-level design. For grouped
designs, \code{n} may be supplied instead of \code{n_per_group}; rows are distributed
as evenly as possible across groups.}

\item{n_groups}{Number of groups. When \code{NULL}, a single-level design is
created.}

\item{n_per_group}{Group sizes. May be a scalar/vector, a function of
\code{n_groups}, or a count-distribution list such as
\code{list(distribution = "poisson", lambda = 4, minimum = 1)}.}

\item{group_id, obs_id}{Character scalars naming the group and observation
index columns.}

\item{time_id}{Optional character scalar naming a time column.}

\item{time_values}{Optional time values. May be a vector shared by units or a
function called per unit/group.}

\item{time_truncate}{Logical; when \code{TRUE}, vector \code{time_values} are truncated
to each group size. When \code{FALSE}, every group must have the same length as
\code{time_values}.}

\item{seed}{Optional random seed. When supplied, the caller's existing random
seed is restored after simulation.}
}
\value{
An object of class \code{mlsim_data}, a list with:
\describe{
\item{\code{data}}{A \code{\link[data.table:data.table]{data.table::data.table()}} containing design columns and
generated variables.}
\item{\code{metadata}}{Simulation design metadata, seed, index metadata, and
generator order.}
\item{\code{generator_specs}}{Generator specifications with simulator closures
removed.}
\item{\code{generator_metadata}}{Per-generator metadata recorded during
simulation.}
}
}
\description{
Build a single-level, grouped, or longitudinal simulation design and evaluate
generator specifications into one shared data table.
}
\details{
Generators are evaluated in list order. Each generator can depend on columns
produced by earlier generators through the simulation context. Generated
column names must be unique and must not collide after \code{make.names()}.
}
\examples{
sim <- simulate_data(
  n_groups = 3,
  n_per_group = 2,
  seed = 10,
  generators = list(
    group_x = gen_mvn("group_x", level = "level2", fixed_intercept = 0, residual_cov = 1),
    y = gen_poisson("y", fixed_intercept = log(2))
  )
)
sim$data
summary(sim)

longitudinal <- simulate_data(
  n_groups = 2,
  n_per_group = 3,
  time_id = "visit",
  generators = list(x = gen_mvn("x", fixed_intercept = 0, residual_cov = 1))
)
longitudinal$metadata$index

}
