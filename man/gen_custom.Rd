% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-generators.R
\name{gen_custom}
\alias{gen_custom}
\title{Generate Variables with a User-Supplied Function}
\usage{
gen_custom(
  vars,
  generator,
  level = c("single", "level2", "multilevel"),
  ...,
  metadata = list()
)
}
\arguments{
\item{vars}{Character vector naming the generated variables.}

\item{generator}{Function called as
\code{generator(context = context, vars = vars, level = level, ...)}. It must
return a list with \code{data} and \code{names} elements, and may include
\code{metadata}.}

\item{level}{Simulation level. A level-2 custom generator may return either
one row per group or one row per simulated observation.}

\item{...}{Additional arguments passed to \code{generator}.}

\item{metadata}{List of wrapper metadata stored on the generator
specification.}
}
\value{
An \code{mlsim_generator_spec} object for use in \code{\link[=simulate_data]{simulate_data()}}.
}
\description{
Wrap a custom generator function so it can participate in \code{\link[=simulate_data]{simulate_data()}}.
}
\examples{
constant_generator <- function(context, vars, level, value = 1) {
  list(data = data.frame(value = rep(value, context$n_rows)), names = vars)
}

sim <- simulate_data(
  n = 3,
  generators = list(x = gen_custom("x", constant_generator, value = 7))
)
sim$data

}
\seealso{
Other predictor generators:
\code{\link{continuous-generators}},
\code{\link{count-generators}},
\code{\link[=gen_categorical]{gen_categorical()}},
\code{\link[=gen_mvn]{gen_mvn()}}
}
\concept{predictor generators}
